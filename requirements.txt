# MMDAutoTrace4 Sh-hei Edition - base dependencies
#
# Colab 2026 note:
# Detectron2 and PHALP are installed separately by install_colab.sh.
# PHALP declares Detectron2 as a Git dependency, which makes pip try to
# rebuild Detectron2 with build isolation even when a compatible Detectron2
# is already installed. Keeping PHALP out of this base requirements file
# avoids that duplicate build.

# 4D-Humans
webdataset==0.2.86
numpy==1.26.4
matplotlib==3.8.4
torchmetrics==1.3.2
lightning-utilities==0.11.2
pytorch-lightning==2.4.0
smplx==0.1.28
pyrender==0.1.45
opencv-python
yacs==0.1.8
scikit-image==0.23.1
einops==0.8.0
timm==1.0.3
dill==0.3.9
pandas==2.2.3
PyOpenGL-accelerate==3.1.7
hydra-core==1.3.2
protobuf==4.21.6

# PHALP is installed separately by install_colab.sh with --no-deps.
# git+https://github.com/miu200521358/PHALP.git

# git+https://github.com/mattloper/chumpy

git+https://github.com/miu200521358/4D-Humans.git

# mlib
Babel==2.13.1
pykalman==0.9.7
