# MMDAutoTrace4 Sh-hei Edition - base dependencies
#
# Colab 2026 note:
# Detectron2, PHALP, Chumpy and 4D-Humans are installed separately by
# install_colab.sh to avoid incompatible isolated dependency builds.
# PyOpenGL-accelerate is intentionally omitted because 3.1.7 fails to build
# on the current Python 3.12 Colab runtime. pyrender installs PyOpenGL itself.

webdataset==0.2.86
numpy==1.26.4
matplotlib==3.8.4
torchmetrics==1.3.2
lightning-utilities==0.11.2
pytorch-lightning==2.4.0
smplx==0.1.28
pyrender==0.1.45
opencv-python
yacs==0.1.8
scikit-image==0.23.1
einops==0.8.0
timm==1.0.3
dill==0.3.9
pandas==2.2.3
hydra-core==1.3.2
protobuf==4.21.6

# Installed separately by install_colab.sh:
# git+https://github.com/miu200521358/PHALP.git
# git+https://github.com/mattloper/chumpy
# git+https://github.com/miu200521358/4D-Humans.git

# mlib
Babel==2.13.1
pykalman==0.9.7
